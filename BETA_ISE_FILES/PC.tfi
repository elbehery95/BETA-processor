


// Instantiate the module
PC instance_name (
    .CLK(CLK), 
    .RESET(RESET), 
    .PCSEL(PCSEL), 
    .ID(ID), 
    .JT(JT), 
    .PC(PC), 
    .PC_INC(PC_INC), 
    .PC_OFFSET(PC_OFFSET)
    );


